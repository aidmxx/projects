# Virtual Scroll Access System

"The Virtual Scroll Access System" is a gateway to the timeless knowledge hidden within ancient scrolls. Designed to manage, preserve, and provide seamless access to these rare repositories of wisdom, the system blends the mystique of the past with the convenience of modern technology.

## Contribution Guideline

Before contributing, please make sure you’ve read and understood the [guidelines](./.github/CONTRIBUTING/guideline.md) to ensure that you are on the right track.

## How to Run The Application?

To run the currency converter application, type the following command into the command line:

```stdin
./gradlew -x test build run --console=plain
```

To run the test for the application, use the following command, it will produce a test report, which can be found in the file `build/reports/jacoco/test/html/`:

```stdin
./gradlew test JacocoTestReport
```